var assert = require("assert");
var Day6 = require("../../Day6/index.js");

describe("Day6", function() {
  describe("Part1()", function() {
    it("should take 5 steps to reach the same input as before when input is [2, 4, 1, 2]", function() {
      assert.equal(5, Day6.Part1([2, 4, 1, 2]));
    });
  });

  /*
  describe("Part2()", function() {
    it("should take XXX steps to reach the same input as before when input is [2, 4, 1, 2]", function() {
      assert.equal(XXX, Day6.Part1([2,4,1,2]));
    });
  });
*/
});
